import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dayrodis.toolbox',
  appName: 'Caja de Herramientas',
  webDir: 'www'
};

export default config;
